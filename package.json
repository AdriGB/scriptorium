{
    "name": "scriptorium",
    "version": "1.2.0",
    "private": true,
    "description": "Editor ligero y offline-first para tarjetas de personaje CharaCard v2 — compatible con PNG y JSON, con boveda IndexedDB y traduccion integrada.",
    "scripts": {
        "build:css": "tailwindcss -i ./assets/tailwind.css -o ./assets/app.css --minify",
        "check": "npm run build:css && esbuild js/app.js --bundle --format=esm --outfile=dist/check.js"
    },
    "devDependencies": {
        "esbuild": "^0.25.0",
        "tailwindcss": "^3.4.17"
    },
    "license": "MIT"
}
