{
  "name": "scriptorium",
  "version": "1.1.0",
  "private": true,
  "description": "Editor ligero y offline-first para tarjetas de personaje CharaCard v2 — compatible con PNG y JSON, con bóveda IndexedDB y traducción integrada.",
  "scripts": {
    "check": "esbuild js/app.js --bundle --format=esm --outfile=dist/check.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "license": "MIT"
}