{
    "name": "scriptorium",
    "version": "1.3.0",
    "private": true,
    "type": "module",
    "description": "Editor ligero y offline-first para tarjetas de personaje CharaCard v2 y v3 — compatible con PNG y JSON, con boveda IndexedDB que conserva el retrato y traduccion integrada.",
    "scripts": {
        "build:css": "tailwindcss -i ./assets/tailwind.css -o ./assets/app.css --minify",
        "test": "node ./tests/regression.mjs && node ./tests/structure.mjs",
        "check": "npm run build:css && esbuild ./js/app.js --bundle --format=esm --outfile=dist/check.js && npm test"
    },
    "devDependencies": {
        "@fortawesome/fontawesome-free": "^6.7.2",
        "esbuild": "^0.25.0",
        "tailwindcss": "^3.4.17"
    },
    "license": "MIT"
}
